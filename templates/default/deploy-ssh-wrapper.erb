#!/usr/bin/env bash
#
# Deploy SSH Wrapper
# Cookbook: magento
#
# Rendered by Chef - local changes will be replaced

/usr/bin/env ssh -o "StrictHostKeyChecking=no" -i "<%= node[:magento][:dir] %>/id_deploy" $1 $2